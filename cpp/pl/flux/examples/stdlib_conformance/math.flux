import "math"

{
    pi_round: math.round(x: math.pi * 100.0) / 100.0,
    abs: math.abs(x: -4.5),
    ceil: math.ceil(x: 2.1),
    floor: math.floor(x: 2.9),
    pow: math.pow(x: 3.0, y: 2.0),
    sqrt: math.sqrt(x: 81.0),
}
