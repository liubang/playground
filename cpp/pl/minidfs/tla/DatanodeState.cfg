\* TLC config for DatanodeState.tla
SPECIFICATION Spec

CONSTANTS
    DataNodes = {dn1, dn2, dn3}
    StaleTimeoutMs = 3
    DeadTimeoutMs = 10
    MaxTime = 20

INVARIANTS
    TypeOK
    StateConsistency
