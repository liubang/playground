syntax = "proto3";

package proto.test.v1;

message Sensor {
  string name = 1;
  double temperature = 2;
  int32 humidity = 3;

  enum SwitchLevel {
    SWITCH_LEVEL_CLOSED = 0;
    SWITCH_LEVEL_OPEN = 1;
  }

  SwitchLevel door = 5;
}
